name: Publish to npm (OIDC)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 3.7.1). Leave blank to use package.json.'
        required: false
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # Node >= 22.14.0 is REQUIRED for npm OIDC trusted publishing.
          # Node 20 completes the token exchange but the registry still 403s
          # the PUT. Node 24 satisfies the requirement.
          node-version: '24'
          cache: 'npm'

      - name: Upgrade npm (OIDC trusted publisher requires npm 11.5.1+)
        # Pin to 11.x — npm@latest is 12.x which needs Node 22+, but this job
        # runs Node 20. npm 11 has OIDC trusted publishing and supports Node 20.
        run: |
          npm install -g npm@11
          npm --version

      - name: Configure npm registry (no auth — OIDC will handle it)
        run: npm config set registry https://registry.npmjs.org/

      - run: npm ci

      - run: npm run build

      - run: npm test

      - name: Update version (if specified)
        if: ${{ github.event.inputs.version }}
        run: npm version ${{ github.event.inputs.version }} --no-git-tag-version

      - name: Debug npm config
        run: |
          npm config list -l 2>&1 | head -40
          echo "---ACTIONS env---"
          env | grep -E "ACTIONS_|GITHUB_REPOSITORY|GITHUB_REF|GITHUB_WORKFLOW" | sort

      - name: Publish to npm (OIDC)
        run: npm publish --provenance --access public --loglevel=verbose
